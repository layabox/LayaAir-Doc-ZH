# LayaAir 文档 — 未修复事项清单(需定夺)

> 生成日期:2026-07-17|核对基线:LayaAir 3.4.0 / LayaAirIDE 3.4.0 / LayaNative3.0(`C:\Users\1\desk\Laya\yuanma`)
> 本轮全量重审 486 篇(除 30 篇 _stubs 空壳),确认错误约 326 条,已机械修复约 250 条。
> **本文件列的是刻意未改的部分**,每条都需要人工或产品侧决定。

---

## 〇、正确性总盘点(截至 2026-07-23,二轮补核后)

**文档实际构成**:总 487 篇 = `_stubs` 占位 30 + `draft:true` 空壳 52 + **实质文档 405**。以下只统计这 405 篇。

| 分类 | 篇数 | 含义 |
|---|---|---|
| **A. 已保证正确** | 385 | 已逐行核对 3.4 源码,确认无误或已修复。个别篇存在"局部待定点"(见 §三/五/六),但主体技术断言核实无误 |
| **B. 已确认失效,待重写** | 3 | `Astar` / `CommandBuffer` / `HTMLDivElement`,已加"过时待重写"横幅(见 §一) |
| **C. 正确性未保证(盲区)** | 17 | 缺源码或按用户要求排除,明细如下 |

**C 类 17 篇明细**:
- **Unity 插件 10 篇**(用户已定不核)—— 引擎侧能核的断言均无误,不确定的仅插件 C# 部分
- **VFX 节点 `context`/`block`/`operator` 3 篇**(用户已定不核)—— 缺 VFX 编辑器插件源码;vfx-graph 其余 6 篇已核
- `released/native/build_Cmd`(`layanative3` 独立 npm 包)
- `released/native/FileSystem`(标注 3.4.1,而快照为 3.4.0)
- `released/Windows/steam`(Steamworks 外部 SDK)
- `LayaDcc_Tool` 第四节(独立仓库 `layadcc2`)

**累计修复**:第一轮全量审 456 篇、修 326 条;二轮补核 4 片再修 4 条,并把大批存疑项坐实为正确。

### 二轮补核(2026-07-23)结果
- **已修 4 条**:`model:280` 骨骼权重"最大"→"最小";`baiduMap:129` `=`→`==`(赋值 bug);`texture:231` 删句尾多余"2";`blueprint/ShaderFunction` title"蓝图函数"→"着色器函数"。
- **坐实为正确**:2D 关节 9 篇全对;assets 导入设置(texture/model/atlascfg/bundledef/Cubemap 默认值与枚举)大批坐实;textureCompress 的 format 数值、statistics 后半 55 项、可核快捷键全对;LayaIdea/AIGC 确认无可核技术断言。
- **新增局部待定**(见下 §五补入):
  1. `IDE/assets/texture:654-656` 第5节"设置服务器纹理资源"**正文被截断**,内容缺失,需作者补全。
  2. `IDE/assets/Cubemap:67-88` 纹理数据模式文档写"RGB / RGBM 模式",但源码 `Asset.ts:427-438` 中这两项已注释、面板现仅 `R8G8B8A8/R16.../R32...`,**源码强烈提示文档过时**,需看面板图 `img/5.png` 确认。
  3. `basics/common/network/WebSocket` 读取示例**通篇使用 @deprecated 的 `getByte/getInt16/getFloat32/getUTFString`**,引擎推荐 `read*` 系列(`Byte.ts:714/722/738/758`);废弃但仍可用,建议统一替换。
  4. `basics/IDE/shortcutKeyCombinations` 的 webview 客户端快捷键(2D/3D 鼠标交互、字母→方向映射、WERT 工具键、时间轴/蓝图节点键)默认值不在可 grep 的注册表中,核不到。
  5. `IDE/uiEditor/textureCompress` 第五章各平台压缩支持矩阵、测试环境记录 —— 属平台事实,源码核不到。
  6. `IDE/assets/model:294` 动画忽略值"以角度为单位"存疑(源码无单位信息);`:61` `normalizeMesh` 中文显示名未在 i18n 坐实。

> **另一类普遍局部待定**:`released` 的 46 篇大量依赖配图,图片内容一律未与正文对照(见 §五第 8 条)。这不影响这些篇的文字断言(文字已核),但配图正确性未保证。

---

## 一、整篇失效,建议废弃或重写(3 篇)

这三篇不是打补丁能救的——所依赖的核心 API 在 3.4 已整体移除。

### 1. `3D/advanced/Astar` — 优先级最高
整篇依赖 `MeshTerrainSprite3D.createFromMeshAndHeightMap(...)`,该类**引擎与 IDE 全库 grep 均 0 结果**。
**铁证**:官方示例 `LayaAir/src/samples/3d/LayaAir3D_Advance/AStarFindPath.ts` **自第 1 行起整个文件被注释掉**,其中 :28/:92/:93 正是该类的声明与调用——文档的"最终代码"是照抄这份已停用示例。
第三章与第五章目前完全不可用。另 :412 `Laya3D.init(0,0)` 也不存在(应为 `Laya.init`)。
**待定**:是否改用引擎已内置的 `laya/navigation/` 模块重写?还是整篇下线?(该篇现方案还依赖第三方 javascript-astar 库)

### 2. `3D/advanced/CommandBuffer`
整篇示例为 2.x 旧代码,已确认 5 类错误:`RenderTextureFormat`/`RenderTextureDepthFormat`(两类型名全库 0 结果)、`setRenderTarget` 单参调用(第2、3参必填)、`clearRenderTarget`(CommandBuffer 无此方法)、`new ShaderData()`(构造函数标 @internal,会抛 NotImplementedError)、`setShaderData`/`setGlobalShaderData`(实为分类型方法)。
**待定**:建议参照官方示例 `CommandBuffer_BlurryGlass.ts` 整篇重写,而非逐条打补丁。

### 3. `2D/displayObject/HTMLDivElement`
整篇基于 `HTMLDivElement` 类,该类 3.4 已移除(全库仅命中 `TextInputAdapter.ts:22` 的 DOM 内置类型)。`HTMLIframeElement`、`contextWidth`/`contextHeight`、`innerHTML` 均随之失效。
**待定**:3.x 的替代方案是 `Text` 组件设 `html=true`(参考 `samples/2d/Text_HTML.ts:36`、`Text.ts:879`)。是否按此方案重写?还是整篇下线?

---

## 二、需判断的段落,已保留原样(6 处)

### 1. `released/miniGame/wechat/index.md:264-275` — MiniAdpter.nativefiles 白名单机制
`nativefiles` 在 LayaAir/src 与 LayaPro/src+bin **grep 均 0 结果**,机制已不存在;所在文件 `laya.wxmini.js` 也不存在(3.x 为 `laya.adapter-weixin.js`)。
**为何没改**:只改文件名会变成"nativefiles 白名单位于 laya.adapter-weixin.js 中"——用一个新的假事实替换旧的假事实。建议连同机制一并重写或下线。

### 2. `IDE/Component/2D/2DAnimation/FrameAnimation/index.md:189-194` — "5.4 重置节点状态"
整节仅由 `frameAni.resetNodes();` 一行构成,该方法全库 grep 0 结果。删除会导致整节消失、章节编号需连带调整。

### 3. `IDE/Component/3DRender/Spine3DRenderer/index.md:256-257` — "获取屏幕坐标"示例
`shaderData.getVector(Sprite3D.WORLDMATRIX)` 取不到屏幕坐标:WORLDMATRIX 注册类型为 Matrix4x4、由 `setMatrix4x4` 写入,而 `getVector` 返回 Vector4(出处 `Sprite3D.ts:51,56`;`ShaderData.ts:330`)。正确的世界坐标转屏幕坐标需确定 API 与示例上下文。

### 4. `IDE/physicsEditor/physics3D/index.md:156,184` + 2.4.3 两张表
`:184` `this.rigidbody1.isTrigger = true;` —— Rigidbody3D 无 isTrigger,仅 `PhysicsCollider` 有(`PhysicsCollider.ts:54`;IDE `D3.ts:2229,2241`,Rigidbody3D 块 :2060 无此属性)。
`:156` "任何碰撞器的触发器属性设置生效后…(刚体触发器)" —— 不成立,不存在"刚体触发器/运动刚体触发器"。2.4.3 两张表据此建立,同受影响。
**为何没改**:涉及重写论述与两张表。

### 5. `2D/advanced/customShader/index.md:467,473,481` — 第八章"三种内置2D着色器"
实际 IDE 创建菜单有 5 项(`ProjectPanel.ts:529-550`),且 `Primitive` 模板**不存在**(菜单项已注释、模板目录无该文件、`D2_primitive` 已废弃);而实际 5 项中的 Particle2D/Trail2D/Spine2D 本章完全没写。属整章结构性改写。

### 6. `IDE/particleEditor3D/readme2.md` 整篇
2022-11-05 旧稿,与 `particleEditor3D/index.md` 同名同题;slug `ide/particleeditor3d/readme2` 会独立出页,但**全仓无任何入站链接**。另 :337/:353 图片指向作者本地 Typora 缓存(`/../AppData/Roaming/Typora/...`),仓库内不存在。
**待定**:是否删除/下线该旧稿?

### 7. `released/miniGame/wechat/Worker/index.md` 整篇 —— 定性已升级为"确认失效"
原判"待定"的理由是"适配库是构建产物 bundle,类名可能被压缩,不能凭 grep 断言 MiniWorkerLoader 不存在"。**该理由已被证伪**:
- 实测 `LayaAir/build/libs/laya.adapter-weixin.js`(56KB)**根本没有压缩**,开头即为带缩进的可读源码(`function ImageDataPolyfill()`、完整错误字符串俱在)
- 计数结果:MiniWorkerLoader 0、createWorker 0、workerMessage 0、WorkerLoader 0、nativefiles 0、MiniAdpter 0(对照 postMessage 命中 2)
- **决定性论证**:`wx.createWorker` 是对微信全局 `wx` 对象的属性访问,**任何压缩器都不能重命名它**,否则代码即失效——它必须在产物中字面出现,而它计数为 0
- 全 libs 目录(LayaAir/build/libs 与 LayaPro/bin/engine/libs)搜 createWorker / MiniWorkerLoader / nativefiles,命中文件数为 **0**;`laya.wxmini.js` 这个文件在两库中根本不存在

即:基类 `Laya.WorkerLoader` 真实存在(`laya/net/WorkerLoader.ts`),但子类 `MiniWorkerLoader` 与 wx.createWorker 集成**假**。
**建议**:与上面第 1 条的 nativefiles 段**同批处理**(同为 2.x 遗留机制),一并重写或下线。

### 8. `basics/PerformanceOptimization/cacheAs/index.md` 整篇(文件零改动)
- `:16,24` "1、cacheAs为normal的缓存优化" **整节**:normal 在 3.4 已非合法取值(`Sprite.ts:868-869` setter 中任何非 bitmap 值等同 none),该节描述的优化整体不成立,且含配图
- `:60` `DebugPanel.init();`:该类全库 grep 0 结果,但 "3.2 测试是否频繁重绘" **整节含配图**都依赖它,删掉即整节失效
**待定**:需重写这两节或设计替代方案(重绘检测在 3.4 的对应工具是什么?)

### 9. `basics/PerformanceOptimization/rendering/index.md:40-46`
cacheAs "三个值可选" + normal 列表项 + `staticCache=true`(全库 grep 0)。需删列表项 2、重写列表项 3(其末句"webGL下命令缓存模式…性能中等"也在讲 normal)、删 staticCache 整句。属改变文档结构,建议与第 8 条一并处理。

### 10. `basics/common/math/BezierCurve/index.md` 整篇
概述 + 第二、三节 + 4.2 节全部建立在不存在的 API 上:`Laya.Bezier.I` 单例(grep 0)、`getPoint2`/`getPoint3`/`insertPoints`(是模块内**私有函数**,未导出、非类成员,无法调用)、`getBezierPoints`(不存在)。
引擎 `Bezier` 类(`laya/maths/Bezier.ts`,全文 141 行)**只有** `static getPoints` 与 `static getRate` 两个成员。
**已修**:仅 `:45` count 语义("每个点的维度"→"曲线阶数")。**其余等于整篇重写**。

### 11. `basics/2.x-Upgrade/index.md:198,200` 整节
"删除了 RIGHT_MOUSE_DOWN 和 RIGHT_MOUSE_UP,改为派发 MOUSE_DOWN 和 MOUSE_UP" —— 该陈述对 3.4 基线为**假**:二者仍存在并被派发(`Event.ts:33,38`;`Script.ts:48-49` 仍注册 onRightMouseDown/onRightMouseUp;本仓 `Component/index.md:299-300` 亦按现存 API 记录)。
需删整节。

---

### 12. `basics/PerformanceOptimization/mouse/index.md` — 示例骨架问题(7 处,已逐行核完但未改)

该篇 265-1102 行本轮已 100% 逐行补核(此前只读了 1-264,实测漏了 10 条,已修)。以下 7 处属结构性问题,未动:

1. `:723,778,824,960`(4 个示例)— `export class MouseInteraction {` 用了 `onEnable()` 生命周期却**没有 `extends Laya.Script`**。横跨 4 个示例,改动等于重写示例骨架。
2. `:972-975` vs `:980` — `addComponent(MouseEventScript)` 但下面定义的类叫 **`SceneScript`**,类名对不上;且 `SceneScript` 用了 `this.text` 却未声明该字段。不确定作者原意用哪个名字。
3. `:980` 等 — 3D 示例里 `Script3D`/`Physics3DUtils`/`Vector2`/`MeshColliderShape` 等**裸用**,既无 `Laya.` 前缀也无 import。全篇风格混用。
4. `:998` — "这里我们基于3D脚本以及**3D的API**进行介绍";改用 InputManager 后此话不再准确(InputManager 是通用 API,非 3D 专属)。
5. `:945` — `onMouseDrag` 称"每帧都在触发";实为 mousemove 时派发(`InputManager.ts:314,448`)。出入轻微。
6. `:821,873` — 外链 `https://ldc2.layabox.com/doc/?nav=zh-ts-4-16-0` 指向 **2.x 旧文档站**,未联网核实有效性。
7. 该篇 `Script3D` 引用成立 — 已验证 `d3/component/Script3D.ts:7` `export var Script3D = Script`(纯别名),生命周期清单与 Script.ts 同源。

> **本条的教训**:这个区间是靠核查代理**如实声明"265 行之后未逐行读"**才被发现的,补核后实测抓到 10 条真错误(含 `Vecntetor3` 拼写、`Input3D` 类不存在、`color` 属性类型错)。其它分片声明的覆盖缺口(见第五节)同理可能藏有漏网。

---

## 三、需人工看图才能结案(1 处)

### `IDE/uiEditor/uiComponent/index.md:19` — "基础UI组件…一共有 17 个"
该句以"如图1-1的**高亮部分**所示"为准,**图片内容无法从源码坐实**。已穷尽两种口径清点,均 ≠ 17:
- **口径A(IDE UI 菜单,非 Box 系)= 16 个**:Image/Clip/Button/CheckBox/Radio/ComboBox/Label/TextInput/TextArea/FontClip/ProgressBar/HSlider/VSlider/HScrollBar/VScrollBar/ColorPicker。此 16 个经核对确均直接/间接继承 UIComponent,**该句的继承性断言对它们成立**。
- **口径B(引擎类树)= 19 个**:上述 16 + 抽象基类 ScrollBar、Slider + 内部类 TipManager。

若把 menu:"UI" 但**不**继承 UIComponent 的 View / Dialog 算入则为 17~18,但那会与本篇 :39「Dialog 并不继承自 UIComponent」自相矛盾。
**建议**:由能查看图 1-1 的人确认高亮了哪 17 个类。
> 附:`:27`「容器对象一共有 9 个」**已坐实为正确**(Box/HBox/VBox/RadioGroup/List/Panel/Tree/Tab/ViewStack),无需改动。

---

## 四、疑似引擎 BUG(文档没错,建议转引擎组)

### `Rigidbody3D.ts:127-132` — gravity 的 setter 调用了 setInertiaTensor
`gravity` 属性的 setter 内部调的是 `this._collider.setInertiaTensor(value)`(**惯性张量**),而非设置重力。
文档 `IDE/Component/physics3D/Rigidbody3D/index.md:277-283` 对"设置刚体重力"的描述本身没错,是实现可疑。**文档未改动**,建议向引擎组确认。

---

## 五、本次核查的盲区(缺源码,无法判定)

以下内容因源码不在 `yuanma` 快照内而**无法核实**,一律未改:

1. **Unity 插件全部 10 篇**(`unity-plugin/*`)—— `find yuanma -name "*.cs"` **0 结果**,C# 插件源码不在快照。涉及 `LayaAir3Export.ExportScene()`、`LayaParticleExportSetting`、`RequiresCPUParticle`、`MetarialPropData.json`、菜单路径、known-issues 全部 14 条。
   > 但引擎侧可验证的断言**无一有误**(renderMode 0-4 映射、LAYAMODEL:0501、24 骨骼上限、BlendFactor 3/7 等均正确)。
2. **`vfx-graph` 的 context.md / block.md / operator.md** —— VFX 编辑器插件(`com.layabox.vfx` / `VfxEditor/`)源码不在快照,LayaPro 仅有编译后的 `laya.vfx.js`,无节点定义表。涉及 typeId/Caption/默认值/显隐规则(operator.md 称 142 个节点)。
3. **`released/native/build_Cmd` 全篇** —— `layanative3` 为独立发布的 npm 包,不在快照。
4. **`released/native/FileSystem` 全篇** —— 本篇标注 "Version >= LayaAir 3.4.1",而三个快照均为 **3.4.0**,功能应落在下一版,不能断言文档错。
5. **`released/Windows/steam`** —— Steamworks SDK 为外部 SDK,仅核了文档内部一致性。
6. **`LayaDcc_Tool` 第四节 LayaDCCClient** —— 文档注明源码在独立仓库 `layadcc2.git` 分支 dccplugin。
7. **`IDE/uiEditor/uiComponent/namingRule` 的资源前缀表** —— LayaPro 中 grep 未命中实现,整张表未验证。
8. **所有图片/截图内容** —— 一律未与正文对照。这是本次覆盖度的普遍缺口,`released` 的 46 篇尤其依赖配图。

---

## 六、"待定"类条目(未计入错误,供参考)

以下三类按"零猜测"标准**不判为文档错误**,故未改:

1. **IDE 面板显示名 ≠ 源码属性名** —— 如 partitionType 的 `Monotone`/`Watershed`、光源模式 `Mixed`/`Realtime`/`Baked`、`Always Animate`、`U_SunSize` 等。文档描述的是面板语境。
2. **@deprecated 但仍存在的 API** —— 如 `fogRange`、`smoothnessTextureScale`、`onMiniGame`、各材质的 `renderMode`。
3. **引擎源码自身的缺陷,文档只是照抄** ——
   - `Scene.ts` 的 JSDoc 12 处写 `autoDestroyAtRemoved`,真实属性是 `autoDestroyAtClosed`
   - `Loader.ts:74-77` 属性名 `noRetry` 但注释写"默认为 true 重试",自相矛盾
   - `Graphics.ts:795-813` 的 jsdoc 缺 `@param minNum`/`@param segPixel`
   - `Material.ts` 的常量拼写 `RENDERMODE_ADDTIVE`(少 I)、`SkyProceduralMaterial.ts:15` 的 `SUN_NODE`
   - `PBRMaterial.ts:41` 注释自称抽象类但 :43 声明未加 `abstract`
   > 这类应向引擎组提 issue,不该由文档单方面"修正"。

---

## 附:上次清单(`文档审查错误清单.md`)的问题

1. **第 28 条为虚假条目**:`3D/advanced/Unity/index.md:592` 的 `BaseMaterial` —— 该篇**全文仅 91 行**,不存在 592 行;`BaseMaterial` 在整个文档站与引擎全库 grep 均 **0 结果**。该条系凭空捏造(疑抄自旧版文档或其它仓库)。
2. **多处错误范围被低估**:`CAPSULE` 标 3 处实有 6 处;`RENDERMODE_*` 标 2 篇实涉 4 篇(其中 Trail/ParticleShuriken 两篇**是对的**,不可误改);`PhysicsComponent` 漏了 `basics/common/Component`;`Laya.WebGL` 漏了 `2D/useWorker`。
3. **行号偏差**:`3D/displayObject` 的 createBox 标 121-123,实际注释块为 120-125。
4. 该清单标注的 117 条修复**从未落盘**,全部滞留在 `git stash@{0}`(标签 `wip-before-vfx-result-push`)中。该 stash 已保留未删,其文档部分已导出备份。
