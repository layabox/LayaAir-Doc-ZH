# LayaAir 文档实用性评分标准（百分制 v2）

> 取消 ABCD,改 0–100 百分制,标准更严。**准确性纳入实用性**。空壳/孤儿属结构问题、**不在本评分内扣分**。
> **结构页(纯链接目录 / 导航 hub,无知识性正文)同样不计分**,单列到「结构页-不计分」表,只记结构问题(断链、漏列子页、路径错)。判据:正文除标题外只有链接列表或子页表格,无独立知识内容。
> 基准:以 Godot 官方文档为"简洁、够用、可跑"的参照系。
> 源码/IDE 交叉核对目录:`C:\Users\1\desk\Laya\yuanma`（LayaAir 引擎 / LayaPro IDE / LayaNative3.0）。

## 一、评分维度（合计 100）

### ① 准确性 + 完整性 —— 35 分（最重）
- **准确 15**:内容与源码/IDE 一致;无错误、无过时/已移除 API、无 2.x/AS3 残留。发现失效 API 直接扣至个位数。
- **完整（漏写现有功能）20**:**以 IDE 暴露为准。** IDE 面板/编辑器里能看到、能用,而文档没讲的,按遗漏数量与重要性扣分。
  - **源码有但 IDE 未暴露的,文档本就不该写,一律不扣分**——包括只存在于源码的 public 属性与运行时方法。
  - 判据:以文档自身的面板截图/属性表能证明 IDE 存在的为准;无法确认 IDE 是否暴露时,不扣分。

### ② 实用性·分人群 —— 20 分
- **新手视角 10**:能否照着上手;概念是否讲清;有无最短上手路径。
- **老手视角 10**:有无深度、选型指导、性能、踩坑;还是只有入门废话。

### ③ 表达效率（对标 Godot）—— 30 分
- **简洁不啰嗦 15**:对照 Godot,是否冗长、重复、堆砌 JSDoc/源码/@param 充数;能一句说清的别铺三段。
- **现代化呈现 15**:**该用可跑 Demo / 视频 / EngineEmbed 替代大量静态截图、以缩短文字量的地方,有没有用现代手段。** 大量 2022 静态图堆砌而未用可跑画面 → 扣。

### ④ 可跑演示 + 实战示例 —— 10 分
- 有无真能跑的演示(EngineEmbed/完整可运行示例);示例是否贴近真实项目而非玩具。

### ⑤ 结构 + 可读 + 关联 —— 5 分
- 层次清晰、循序渐进;交叉链接不孤岛;语言通顺、术语一致。

## 二、分数线
| 区间 | 含义 |
|---|---|
| 90–100 | 标杆(可作范本) |
| 75–89 | 良好 |
| 60–74 | 及格(能用但有明显短板) |
| 40–59 | 差(缺漏/啰嗦/过时多项) |
| <40 | 需重写 |

## 三、修改优先级（每篇标注）
- **P0 必改**:失效/错误,或"源码有功能而文档漏写"的大缺漏。**只要满足其一就必须是 P0,与分数高低无关**(73 分也可以是 P0)。
- **P1 应改**:啰嗦冗长、缺可跑演示、缺深度/踩坑、大量静态图可现代化。
- **P2 可优化**:小修补、配图更新、补交叉链接。

**硬约束**:分数 < 60(即「差」及以下)的文档**不得标 P2**,至少 P1。

## 四、输出格式（每篇一行）
`分数 || 准确+完整35 || 实用性20 || 表达效率30 || 可跑演示10 || 结构5 || 优先级 || 模块 || 文档标题 || 路径 || 扣分项`

- 五个维度分**必须加总等于总分**。
- 扣分项只写扣分理由,**不写褒义评语**(「好」「表格清晰」等等于没写)。例:`漏写 setXxx/事件回调(源码有);40+张静态图未用可跑演示;老手向深度不足`。
