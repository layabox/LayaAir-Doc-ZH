---
description: LayaAir 文档编写规范：章节编号、标题层级、图片编号/路径/引用
globs: "**/*.{md,mdx}"
alwaysApply: true
---

# 文档编写规范

编写或续写本项目文档时必须遵守以下规则。

## 1、章节编号与 Markdown 标题层级

所有章节采用**阿拉伯数字**递进编号。一级章节用顿号「、」收束；不得使用中文序号。

**重要：正文不要使用单个 `#`。** 页面标题由 frontmatter 的 `title` 生成（即文档 H1）。正文一级章节从 `##` 开始，否则会与文档标题冲突，导致右侧章节导航异常。

| 文档层级 | 编号格式 | Markdown |
|----------|----------|----------|
| 页面标题 | （frontmatter） | `title: "…"`，勿在正文再用 `#` |
| 一级章节 | 1、2、3 | `## 1、标题` |
| 二级 | 1.1、1.2 | `### 1.1 标题` |
| 三级 | 1.2.1、1.2.2 | `#### 1.2.1 标题` |
| 四级 | 1.2.1.1 | `##### 1.2.1.1 标题` |

示例：

```markdown
---
title: "IDE主要模块概述"
---

## 1、IDE帐号登录

### 1.1 登录方式

#### 1.1.1 微信扫码
```

**选用说明（必须遵守）：**

- **一级用 `## 1、`**：顿号补全中文观感；`##` 保证目录层级正确。
- **不用 `一、二、三、`**：与 `1.1`、图号「图1-1」不一致。
- **二～四级不写顿号**：`1.1` 后空一格再写标题。

禁止：无编号标题；正文 `#` 一级标题；`一、` / `（一）` / `A.`；一级仅数字无顿号（如 `## 1 标题`）。

## 2、图片编号

图片编号按**一级章节**分组；**每一章从 1 起连续编号**，不得跳号、不得沿用历史旧号。

- 第 1 章第 1 张：图1-1；第 2 张：图1-2…
- 第 4 章只有 1 张图时：必须是 **图4-1**（不得写成图4-6）
- 第 8 章有 4 张图时：图8-1、图8-2、图8-3、图8-4（不得写成图8、图8-1-2）

格式固定为：**图{章号}-{序号}**（两段，中间一个连字符）。

## 3、图片文件路径

图号与文件名必须一一对应，放在与文档配套的 `img/` 目录下：

| 图号 | 正确路径 | 错误示例 |
|------|----------|----------|
| 图4-1 | `./img/4-1.png` | `./img/4-6.png` |
| 图8-3 | `./img/8-3.png` | `./img/8-1-2.png`、`./img/8.png` |
| 图9-1 | `./img/9-1.png` | `./img/9.png` |

调整图号时须**同步重命名图片文件**，保证正文引用、图题、文件名三者一致。

## 4、图片标题

每张图片下方必须写标题，格式固定为全角括号：

```markdown
![图4-1](./img/4-1.png)

（图4-1）
```

## 5、正文引用图片

统一使用「如图X-Y所示……」，例如：如图1-1所示、如图4-1所示。

禁止：「下图」「如下图」「见图片」「见截图」。

## 6、暂无图片时

```markdown
> 【插入图片：此处填写图片内容描述】

（图1-1）
```

（占位的图号仍须符合第 2 条连续编号，对应文件名预留给 `./img/1-1.png` 一类路径。）

## 7、续写文档

- 延续已有章节编号，不得从 1 重新开始
- 延续当前一级章节内已有图片编号（下一张 = 当前最大序号 + 1）
- 先读取目标文档与 `img/` 目录现状，再落笔
