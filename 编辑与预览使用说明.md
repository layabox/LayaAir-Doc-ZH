# LayaAir 文档 · 编辑与预览使用说明

面向文档编辑人员。本地预览模式自带页内编辑器,**边改边看,所见即所得**;正式构建的网站里不含任何编辑功能。

## 一、启动预览

```
cd htmldoc
npm run dev
```

浏览器打开 `http://localhost:4321` 即可浏览全部文档(包括尚未上线的草稿页)。

> 首次使用需先安装 Node.js,并在 htmldoc 目录执行一次 `npm install`。

## 二、页内编辑

打开任意文档页,右下角有「**编辑本页**」按钮:

| 操作 | 方法 |
|------|------|
| 打开编辑 | 点右下角「编辑本页」,右侧滑出编辑框,页面自动让位不遮正文 |
| 实时预览 | 直接打字,停顿半秒左侧正文自动更新(未落盘) |
| 保存 | **Ctrl+S** 或点「保存」,写入源文件,页面不刷新 |
| 撤回 | 点「撤回」或 **Ctrl+Z**,打字、插图、换图都能一步步退 |
| 放弃修改 | 直接点「关闭」——**不保存就不修改**,文字和图片全部还原 |
| 调面板宽度 | 拖编辑框左边缘 |

### 定位:编辑处 ↔ 预览处

在编辑框里**双击一个词**或**划选一段文字**,左侧预览自动滚到对应位置并黄底高亮;
双击图片引用的任意部分,则直接定位到那张图(黄框标出)。

### 图片

- **新加图**:光标停在插入点,直接 **Ctrl+V 粘贴截图**或把图片文件拖进编辑框——自动按本页序号命名(如已有 1~6 号则存为 `7.png`)并插入引用
- **换图**:先**三击选中**正文里那条 `![](…)` 图片引用整行,再粘贴——新图沿用原序号原地覆盖,引用不变
- 换图可反悔:「撤回」/Ctrl+Z/状态栏「还原原图」/不保存关闭,都能回到原图
- 保存时自动清理:粘贴后又删掉引用的图片,文件会一并删除,不留垃圾

## 三、草稿页(未完成文档)

尚未编写完成的文档分两种,都**不会出现在正式版目录里**:

1. **draft 页**:frontmatter 里有 `draft: true`。正式构建时整页不输出;本地预览照常可见可编辑
2. **占位页**:frontmatter 里有 `pagefind: false`(正文显示"内容整理中")。因为被其他文档链接着,正式版保留页面防止死链,但不进目录、不进搜索

**写完一篇后的上线动作**:删掉 frontmatter 里的 `draft: true`(或占位页的 `pagefind` / `head` 那几行),替换正文——下次构建时页面和目录条目自动恢复,无需改其他任何文件。

## 四、构建上线

```
npm run build
```

产物输出到 `htmldoc/_book/`(与旧版 GitBook 目录名一致)。构建自动完成:
草稿页排除、目录裁剪、全站图片懒加载。编辑器代码不会进入产物。

### 辅助工具(htmldoc/tools/)

| 脚本 | 用途 |
|------|------|
| `node tools/verify-content.mjs` | 校验构建产物与源文档内容一致(先 build) |
| `node tools/audit-links.mjs` | 检查产物内部死链(先 build) |
| `tools/migrate.mjs` | ⚠️ 一次性迁移脚本,勿重跑(见文件头注释) |

页内编辑器的技术说明见 `dev-editor/README.md`。
